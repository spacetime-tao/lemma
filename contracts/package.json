{
  "name": "lemma-bounty-escrow",
  "private": true,
  "version": "0.1.0",
  "description": "Bittensor EVM escrow contracts for Lemma theorem bounties",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "export-abi": "node scripts/export-abi.js",
    "deploy:testnet": "hardhat run scripts/deploy.js --network bittensorTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network bittensorMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
